# Composable

> 'composable' means you can combine small pieces to build something bigger. Keep up the awesome work!
> <br>
> <br>

# Declarative

> Can lean on the library to handle the manual, tedious tasks that we otherwise would have to worry about overselves

# Render

> React runs your function and displays whatever gets returned. The function wil only be run again if:
> (1) it receives new props from above, or
> (2) it's internal state values changes.

# setState
> Changing a local, non-state variable doesn't cause react to re-render the component. Changing state with a built-in "setState" function does. 

# view = function(state)
> Thus, when state changes and react re-runs(re-renders) your component, something new gets returned and replaces what used to be on the page