document.querySelector('input[type="password"]')
document.querySelector('p:first-child')

// node list is not a pure array

// converting in the arrayy 
let myarr 
Array.from(myarr)

const h2 = document.querySelectorAll(".mw-heading2")
h2.forEach(function (h) {
    h.style.color = "#f9dada";
    h.style.backgroundColor = "peachpuff";
    h.style.padding = "1rem";
    h.style.borderRadius = "0.5rem"
    h.innerText = "🧀"
})